import { createPlaceholderRouter } from '../utils/createPlaceholderRouter';

// Real routes (in-app notifications) land here in a later phase.
export default createPlaceholderRouter('Notifications');
