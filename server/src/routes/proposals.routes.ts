import { createPlaceholderRouter } from '../utils/createPlaceholderRouter';

// Real routes (submit/list proposals) land here in the Proposals phase.
export default createPlaceholderRouter('Proposals');
