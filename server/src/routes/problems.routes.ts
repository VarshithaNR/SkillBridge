import { createPlaceholderRouter } from '../utils/createPlaceholderRouter';

// Real routes (CRUD for problems, search/filter) land here in the Problems phase.
export default createPlaceholderRouter('Problems');
