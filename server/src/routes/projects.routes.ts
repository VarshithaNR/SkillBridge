import { createPlaceholderRouter } from '../utils/createPlaceholderRouter';

// Real routes (project workspace creation/management) land here in a later phase.
export default createPlaceholderRouter('Projects');
